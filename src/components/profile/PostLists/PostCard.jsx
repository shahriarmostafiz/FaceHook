import React from 'react';
import PostHeader from './PostHeader';

const PostCard = ({ post }) => {
    return (
        <div>
            <PostHeader />
        </div>
    );
};

export default PostCard;